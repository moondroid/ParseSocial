<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.moondroid.parsesocial" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="it.moondroid.parsesocial.ParseSocialApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="it.moondroid.parsesocial.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="it.moondroid.parsesocial.SaveObjectActivity"
            android:label="@string/title_activity_send_object" >
        </activity>
        <activity
            android:name="it.moondroid.parsesocial.ReadObjectsActivity"
            android:label="@string/title_activity_read_objects" >
        </activity>
        <activity
            android:name="it.moondroid.parsesocial.LoginActivity"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity
            android:name="it.moondroid.parsesocial.AllPostsActivity"
            android:label="@string/title_activity_all_posts" >
        </activity>
    </application>

</manifest>
